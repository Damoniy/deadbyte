package net.deadbyte.widgris.engine.animation

import net.deadbyte.widgris.engine.resources.Sprite
import java.awt.image.BufferedImage

class Animation {
   // private val sprite: Sprite = Sprite()
   // private val count: Int = 0
   // private val frames: Array<BufferedImage> = Array(count) { sprite.image }
}